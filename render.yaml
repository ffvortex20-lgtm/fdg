services:
  - type: web
    name: meu-projeto-python
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py # Substitua pelo arquivo principal do seu script

